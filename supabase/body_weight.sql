-- Body weight log (v3 migration — run after schema.sql and routines.sql).
-- One row per weigh-in. Same access model as everything else: RLS on, no
-- public policies, all access through the API's service-role key.


